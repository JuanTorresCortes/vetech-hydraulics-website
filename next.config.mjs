// Keep framework configuration minimal; routing, images, and SEO behavior are handled in pages/components unless a future Next.js option is truly needed.
/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
